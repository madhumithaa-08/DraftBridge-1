FROM node:20-alpine

WORKDIR /app

# Copy package files for dependency installation
COPY package*.json ./

# Install all dependencies
RUN npm install

# Source code will be bind-mounted from host for hot-reload

# Expose Next.js default port
EXPOSE 3000

# Development command with hot-reload
CMD ["npm", "run", "dev"]
